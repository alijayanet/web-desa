<!-- Sidebar Navigation -->
<div class="sidebar">
  <div class="sidebar-logo">
    <div class="sidebar-logo-icon <%= settings.logo ? 'has-logo' : '' %>" style="display: flex; align-items: center; justify-content: center; overflow: hidden; border-radius: var(--radius-sm);">
      <% if (settings.logo) { %>
        <img src="<%= settings.logo %>" alt="Logo" style="max-width: 100%; max-height: 100%; object-fit: contain;">
      <% } else { %>
        <%= settings.nama_desa ? settings.nama_desa.replace('Desa ', '').charAt(0) : 'U' %>
      <% } %>
    </div>
    <div class="sidebar-logo-text">
      <h3><%= settings.nama_desa || 'Desa Ujunggebang' %></h3>
      <p>Kecamatan <%= settings.kecamatan || 'Sukra' %></p>
    </div>
  </div>

  <div class="sidebar-user">
    <div class="sidebar-avatar">
      <%= user.nama ? user.nama.charAt(0).toUpperCase() : 'U' %>
    </div>
    <div class="sidebar-userinfo">
      <h4><%= user.nama || 'Pengguna' %></h4>
      <p><%= (user.role === 'kades' || user.role === 'kuwu') ? (settings.sebutan_kades || 'Kepala Desa') : (user.role === 'sekdes' ? 'Sekdes' : (user.role === 'staff' ? 'Staff Desa' : (user.role === 'warga' ? 'Warga' : user.role))) %></p>
    </div>
  </div>

  <ul class="sidebar-menu">
    <% const roleLower = user && user.role ? String(user.role).toLowerCase() : ''; %>
    <li>
      <a href="/dashboard" class="sidebar-link">
        <span>🏠</span> Ringkasan
      </a>
    </li>
    <% if (roleLower !== 'warga') { %>
      <li>
        <a href="/dashboard/absensi" class="sidebar-link">
          <span>📷</span> <%= roleLower === 'staff' ? 'Absen Saya' : 'Absensi' %>
        </a>
      </li>
      <% if (roleLower === 'sekdes' || roleLower === 'kades' || roleLower === 'kuwu') { %>
        <li>
          <a href="/dashboard/absensi?view=monitor" class="sidebar-link">
            <span>📊</span> Laporan Absensi
          </a>
        </li>
      <% } %>
    <% } %>

    <!-- MENU KHUSUS WARGA -->
    <% if (user.role === 'warga') { %>
      <li>
        <a href="/dashboard/profil-saya" class="sidebar-link">
          <span>👤</span> Data Kependudukan
        </a>
      </li>
      <li>
        <a href="/dashboard/surat" class="sidebar-link">
          <span>✉️</span> Pengajuan Surat Online
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaduan" class="sidebar-link">
          <span>📢</span> Laporan Pengaduan
        </a>
      </li>
    <% } %>

    <!-- MENU KHUSUS STAFF -->
    <% if (user.role === 'staff') { %>
      <li>
        <a href="/dashboard/penduduk" class="sidebar-link">
          <span>👥</span> Manajemen Penduduk
        </a>
      </li>
      <li>
        <a href="/dashboard/surat/kelola" class="sidebar-link">
          <span>✉️</span> Verifikasi &amp; TTD Surat
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaduan/kelola" class="sidebar-link">
          <span>📢</span> Tanggapi Pengaduan
        </a>
      </li>
      <li>
        <a href="/dashboard/berita" class="sidebar-link">
          <span>📰</span> Manajemen Berita
        </a>
      </li>
      <li>
        <a href="/dashboard/keuangan" class="sidebar-link">
          <span>📊</span> Keuangan APBDes
        </a>
      </li>
      <li>
        <a href="/dashboard/kategori" class="sidebar-link">
          <span>🏷️</span> Kelola Kategori
        </a>
      </li>
    <% } %>

    <!-- MENU KHUSUS SEKDES -->
    <% if (user.role === 'sekdes') { %>
      <li>
        <a href="/dashboard/penduduk" class="sidebar-link">
          <span>👥</span> Manajemen Penduduk
        </a>
      </li>
      <li>
        <a href="/dashboard/surat/kelola" class="sidebar-link">
          <span>✉️</span> Verifikasi &amp; TTD Surat
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaduan/kelola" class="sidebar-link">
          <span>📢</span> Tanggapi Pengaduan
        </a>
      </li>
      <li>
        <a href="/dashboard/berita" class="sidebar-link">
          <span>📰</span> Manajemen Berita
        </a>
      </li>
      <li>
        <a href="/dashboard/keuangan" class="sidebar-link">
          <span>📊</span> Keuangan APBDes
        </a>
      </li>
      <li>
        <a href="/dashboard/kategori" class="sidebar-link">
          <span>🏷️</span> Kelola Kategori
        </a>
      </li>
      <li>
        <a href="/dashboard/pegawai" class="sidebar-link">
          <span>💼</span> Manajemen Pegawai
        </a>
      </li>
      <li>
        <a href="/dashboard/templates" class="sidebar-link">
          <span>📝</span> Template Surat
        </a>
      </li>
      <li>
        <a href="/dashboard/whatsapp" class="sidebar-link">
          <span>💬</span> WhatsApp Bot
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaturan" class="sidebar-link">
          <span>⚙️</span> Pengaturan Profil Desa
        </a>
      </li>
    <% } %>

    <!-- MENU KHUSUS KEPALA DESA -->
    <% if (user.role === 'kades') { %>
      <li>
        <a href="/dashboard/surat/kelola" class="sidebar-link">
          <span>✉️</span> Verifikasi &amp; TTD Surat
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaduan/kelola" class="sidebar-link">
          <span>📢</span> Tanggapi Pengaduan
        </a>
      </li>
      <li>
        <a href="/dashboard/berita" class="sidebar-link">
          <span>📰</span> Manajemen Berita
        </a>
      </li>
      <li>
        <a href="/dashboard/keuangan" class="sidebar-link">
          <span>📊</span> Keuangan APBDes
        </a>
      </li>
      <li>
        <a href="/dashboard/kategori" class="sidebar-link">
          <span>🏷️</span> Kelola Kategori
        </a>
      </li>
      <li>
        <a href="/dashboard/pegawai" class="sidebar-link">
          <span>💼</span> Manajemen Pegawai
        </a>
      </li>
      <li>
        <a href="/dashboard/templates" class="sidebar-link">
          <span>📝</span> Template Surat
        </a>
      </li>
      <li>
        <a href="/dashboard/whatsapp" class="sidebar-link">
          <span>💬</span> WhatsApp Bot
        </a>
      </li>
      <li>
        <a href="/dashboard/pengaturan" class="sidebar-link">
          <span>⚙️</span> Pengaturan Profil Desa
        </a>
      </li>
    <% } %>
  </ul>

  <div class="sidebar-footer">
    <a href="/logout" class="sidebar-link" style="color: #ef4444; background: rgba(239, 68, 68, 0.05); justify-content: center; border: 1px solid rgba(239, 68, 68, 0.1);">
      <span>🚪</span> Keluar Sesi
    </a>
  </div>
</div>

<script>
  // Aktifkan link sidebar sesuai URL aktif (termasuk sub-path matching)
  document.addEventListener('DOMContentLoaded', () => {
    const currentPath = window.location.pathname;
    const links = document.querySelectorAll('.sidebar-link');
    links.forEach(link => {
      const href = link.getAttribute('href');
      if (!href) return;
      // Exact match untuk /dashboard agar tidak match semua
      if (href === '/dashboard') {
        if (currentPath === '/dashboard') link.classList.add('active');
      } else {
        // Prefix match untuk sub-path (misal /dashboard/surat/kelola match /dashboard/surat)
        if (currentPath === href || currentPath.startsWith(href + '/') || currentPath.startsWith(href + '?')) {
          link.classList.add('active');
        }
      }
    });
  });
</script>
